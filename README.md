# Chrome 标签页管理器

简体中文 | [English](README.en.md)

一个运行在 Chrome Side Panel（侧边栏）中的本地标签页管理扩展。它可以搜索、筛选、切换和关闭标签页，并支持普通列表、可注册主域名分组、实时同步以及全局/单标签页隐私隐藏。首次打开时内容默认可见，之后会恢复用户保存的界面偏好。

## 主要功能与界面

- **搜索与筛选**：可按标签页标题或 URL 搜索，并按全部标签页、当前窗口等条件筛选。即使某个标签页处于隐藏状态，搜索仍可在内存中的原始数据上工作，但结果不会暴露其标题、URL 或 favicon。
- **标签页操作**：点击条目可切换到对应标签页并聚焦目标窗口；可直接关闭标签页；标签页和窗口的创建、更新、移动、激活与关闭会实时同步。
- **两行紧凑工具栏与渐进吸顶**：第一行使用浅蓝色数字胶囊显示标签页数量，并将“全部 / 当前 / 活动”（英文为“All / Current / Active”）放入浅灰色分段胶囊；三个短标签始终可见，选中项使用白底蓝字。语言、全局隐私和分组折叠按钮独立靠右排列。第二行由可伸缩搜索框和列表/分组图标按钮组成。向下滚动时第一行淡出并收起，完整搜索行持续吸顶；回到顶部时按实际滚动位置逐渐恢复。
- **仅图标隐私控件**：全局、分组和单个标签页的显示/隐藏操作只显示眼睛状态图标，不显示按钮文字。按钮仍提供随状态变化的 `aria-label`，并支持键盘操作和清晰的焦点样式。

### 列表与域名分组

- **首次默认使用列表视图**，保持按 Chrome 窗口展示标签页的方式。
- 用户可以切换到**域名分组视图**；所选视图会保存到本地，下次打开 Side Panel 时恢复。
- 域名分组基于 Public Suffix List 解析**可注册主域名**，而不是简单截取 hostname 的最后两段。例如：
  - `docs.google.com`、`drive.google.com`、`www.google.com`、`xaa.bac.google.com` 都归入 `google.com`。
  - `docs.example.com.cn` 归入 `example.com.cn`。
- 解析时启用 Public Suffix List 的 Private Section，避免把不同租户误归为同一个网站。例如 `alice.github.io` 与 `bob.github.io` 会分别归组，而不是都归入 `github.io`；`blogspot.com`、`s3.amazonaws.com` 等私有后缀租户同理。
- `localhost`、IP 地址、Chrome 页面、扩展页面、文件页面和无法解析的 URL 会使用稳定的特殊分组。
- 分组基于当前搜索与筛选结果生成，可跨 Chrome 窗口合并，并支持单组展开/折叠；分组折叠状态只在当前页面内存中保留。
- 域名分组工具栏提供一个 32px 折叠/展开图标按钮：任一当前可见分组展开时点击会折叠全部当前可见分组；全部当前可见分组已折叠时点击会展开全部当前可见分组。
- 分组标题提供眼睛图标，可对该分组中**当前搜索/筛选后可见的标签页**统一隐藏或显示；不会改变全局隐藏开关。
- 分组标题提供关闭图标，关闭前会弹出二次确认并显示当前可见标签页数量；确认后只关闭该分组中当前搜索/筛选后可见的标签页。
- 如果一个域名组内的标签页全部处于隐藏状态，分组标题会改为通用名称，避免从分组标题推断被隐藏的域名。

### 关闭 Side Panel

- Side Panel 靠网页一侧的垂直中间提供一个约 18px 宽的箭头按钮，用于快速关闭当前窗口的 Chrome Side Panel。
- 点击该箭头会调用 Chrome 的 Side Panel API 关闭侧边栏；关闭后扩展页面不会再保留内部空白抽屉或网页内恢复箭头，网页可恢复使用 Chrome 释放的显示区域。
- 需要重新打开时，请点击 Chrome 工具栏中的扩展图标。
- 如果当前 Chrome 环境暂不支持关闭 API，或关闭请求失败，界面只显示通用失败提示，不会暴露底层异常信息。

## 环境要求

- Chrome **141 或更高版本**
- Node.js 与 npm

## 安装、测试与构建

在项目根目录依次运行：

```bash
npm install
npm test
npm run lint
npm run build
```

命令说明：

- `npm install`：安装项目依赖。
- `npm test`：运行完整自动化测试。
- `npm run lint`：运行 ESLint 静态检查。
- `npm run build`：先执行 TypeScript 检查和 Vite 生产构建；待 Vite 完全退出并完成所有构建钩子后，再运行独立的最终 `dist` 校验命令。Chrome 可加载的输出目录为 `dist`，任一阶段失败都会使命令失败。

## 在 Chrome 中加载扩展

1. 先运行 `npm run build`，确保已生成最新的 `dist` 目录。
2. 在 Chrome 地址栏打开 `chrome://extensions`。
3. 打开页面右上角的「开发者模式」。
4. 点击「加载已解压的扩展程序」（Load unpacked）。
5. 选择以下**绝对路径**：

   ```text
   <项目目录>/dist
   ```

6. 可在 Chrome 工具栏的扩展菜单中固定「标签页管理器」。
7. 点击扩展图标打开 Side Panel，然后在侧边栏中使用标签页管理功能。

每次重新执行 `npm run build` 后，可回到 `chrome://extensions`，点击此扩展的刷新按钮以加载最新构建。

## 权限说明

扩展仅声明以下 Chrome 权限：

- `tabs`：读取标签页的标题、URL、图标、活动状态与所属窗口，并执行切换或关闭标签页操作。
- `favicon`：通过 Chrome 提供的扩展本地 `/_favicon/` 端点显示网站图标，避免扩展页面直接请求网站的 favicon URL。
- `sidePanel`：在 Chrome Side Panel 中显示扩展界面，并通过箭头按钮关闭当前窗口的 Side Panel。

扩展**不声明任何 host permissions（主机权限）**，不会读取或修改网页正文，也不会向网站注入脚本。

## 数据与远程资源

- 标签页数据仅通过 Chrome 扩展 API 在本地读取和使用。
- 扩展不会把标签页标题、URL、favicon 或其他数据传输到远程服务器。取消隐藏后，网站返回的 `http:`/`https:` favicon 地址不会直接写入图片 `src`，而是转换为 Chrome 扩展自身的 `/_favicon/` 本地端点；受限的内联位图 `data:` 来源可直接显示。
- manifest 为扩展页面声明了显式 CSP：脚本和样式仅允许扩展自身资源，图片仅允许扩展自身资源与受限 `data:` 图片，并禁止远程连接、对象和 frame，因此 `img-src` 不允许 `http:`/`https:`。
- 构建产物不依赖远程脚本、样式、字体、图片或其他远程资源；界面资源均随扩展本地打包。
- 生产构建会检查所有已知文本产物（包括以 UTF-8 字节输出的 HTML、CSS、JavaScript、JSON、SVG、XML 与 TXT 等文件），拒绝任何意外残留的明文 `http://` 或 `https://`。JavaScript 中仅对 React 已知诊断地址和 W3C 命名空间常量进行精确字符串字面量转义，运行时值保持不变；独立 SVG/XML 中的标准 W3C 命名空间标识符可保留，但若它们出现在可加载资源属性中仍会被拒绝。
- HTML 使用 `parse5` 按 HTML 语法解析，校验浏览器可加载资源属性，包括 `script`/`img`/`iframe`/`embed`/`video`/`audio`/`source`/`track`/`input` 的 `src`、`link` 等元素的 `href`、`video[poster]`、`form[action]`、`button`/`input[formaction]`、`object[data]`、`body`/`table`/`td`/`th[background]`、`img`/`source[srcset]`、meta refresh URL，并递归解析 `iframe[srcdoc]`；`<style>` 和 `style` 属性也会作为 CSS 校验。
- CSS 使用 `css-tree` 解析 `url()`、`@import`，以及 `image-set()`/`-webkit-image-set()` 中作为图片候选的字符串；函数标识符和 `@import` at-rule 标识符会先解码 CSS 转义，再识别标准及 WebKit 名称，同时覆盖资源值中 CSS 转义后的绝对协议、协议相对地址与路径穿越。独立 `.svg`/`.xml` 使用 `@xmldom/xmldom` 严格解析，校验 `href`/`xlink:href`/`src`/`data`/`background`/`srcset`、内联 CSS、XML stylesheet 处理指令，以及本地 `xml:base` 的继承解析；远程、协议相对或逃逸扩展根目录的 `xml:base` 会被拒绝。HTML、CSS 或 XML 解析错误，外部协议或逃逸扩展包根目录的路径都会使构建失败；允许扩展包内路径及内联 `data:`/`blob:` 数据。
- manifest 资源字段同样只能引用扩展包内路径。Vite 打包阶段会检查直接生成的 bundle，Vite 完全退出后还会由独立命令递归检查最终 `dist`，因此复制到输出目录的文件也在检查范围内。
- 当前非文本构建产物仅允许 `icons/icon-16.png`、`icon-32.png`、`icon-48.png` 和 `icon-128.png`；构建会校验 PNG 文件签名及对应尺寸。其他未知二进制类型或路径会使构建失败。

## 隐私隐藏行为

### 首次默认显示与全局偏好

首次打开扩展时，所有标签页的标题、URL 和 favicon 默认可见。用户通过顶部眼睛图标切换全局隐藏状态后，该**全局隐私状态会保存到本地**，下次打开或重新加载 Side Panel 时恢复最后一次选择。

隐藏状态下，界面不会把标签页的原始标题、URL 或 favicon 渲染到对应标签行的可见 DOM 或无障碍信息中。搜索与筛选仍可基于内存中的标签页数据工作，但匹配结果只显示通用占位内容，不会因此泄露原值。域名分组中，如果整组标签页均被隐藏，分组标题也会使用通用名称。

### 全局切换

顶部的全局眼睛图标会统一切换所有标签页的隐私状态。每次执行全局切换时，都会清除此前所有单个标签页的覆盖设置，使全部标签页重新服从当前全局状态。

### 单个标签页覆盖

每个标签页行都有独立的眼睛图标，可以只显示或隐藏该标签页。域名分组标题也有独立的眼睛图标，可以批量隐藏或显示当前可见分组内的标签页。单个标签页和分组操作产生的设置只覆盖对应标签页，不影响其他标签页，也不会改变全局开关。

**只有全局隐私状态会持久化。**单个标签页与分组批量操作产生的隐私覆盖只保存在当前 Side Panel 页面内存中，不会写入 `localStorage` 或其他持久化存储；重新加载、关闭后重新打开，或页面被 Chrome 重建后，这些覆盖会清除，并重新服从已保存的全局状态。这也避免 Chrome 复用 Tab ID 时把旧设置错误应用到新页面。

### 本地保存范围

扩展使用版本化的本地 UI 偏好，仅保存以下界面状态：

- 全局隐私显示/隐藏状态
- 列表或域名分组视图
- 显示语言偏好（自动、简体中文或英文）

不会保存标签页标题、URL、favicon、搜索内容、分组展开状态、单个标签页的隐私覆盖、分组批量隐私覆盖或 Side Panel 关闭状态。旧版本保存过的已废弃字段会被兼容读取并忽略。存储内容无效、版本不兼容或浏览器拒绝读写时，扩展会安全回退到首次默认状态：内容可见、列表视图、语言自动跟随浏览器。

## 语言与国际化

- Side Panel 界面支持简体中文和英文，首次使用默认跟随浏览器语言。
- 浏览器语言中的任意 `zh`/`zh-*` 会使用简体中文，任意 `en`/`en-*` 会使用英文；不支持或无法识别的语言安全回退为英文。
- 顶部工具栏使用 32px 地球图标打开语言菜单，可选择“自动（浏览器语言）”、中文或 English。菜单支持方向键、Home/End、Escape 和点击外部关闭；切换即时生效，不会重置搜索、筛选、视图、折叠或隐私状态。
- 手动语言选择会随现有 UI 偏好保存在本地；单标签页隐私覆盖仍只保留在当前页面内存中。
- 扩展名称、描述和工具栏提示使用 Chrome `_locales` 标准资源，由 Chrome 按浏览器语言选择；Side Panel 内的手动语言不会改变扩展管理页元数据语言。
